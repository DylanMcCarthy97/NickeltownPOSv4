<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="NickeltownPOSV4.Controls.PosWorkspaceHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="56">

    <Grid Padding="8,0,8,0" Background="{StaticResource PosHeaderBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center">
            <TextBlock
                FontSize="13"
                FontWeight="SemiBold"
                Foreground="{StaticResource PosOnDarkSecondaryForegroundBrush}"
                Text="Nickeltown POS" />
            <TextBlock
                FontSize="11"
                FontWeight="SemiBold"
                Foreground="{StaticResource PosOnDarkForegroundBrush}"
                Text="{x:Bind Subtitle, Mode=OneWay}" />
        </StackPanel>

        <Image
            Grid.Column="1"
            Width="80"
            Height="80"
            Margin="0,-10,0,-10"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsHitTestVisible="False"
            Source="ms-appx:///Assets/FlounderersLogo.png"
            Stretch="Uniform" />

        <ContentPresenter
            Grid.Column="2"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Content="{x:Bind RightContent, Mode=OneWay}" />
    </Grid>
</UserControl>