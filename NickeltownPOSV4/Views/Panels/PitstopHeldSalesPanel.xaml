<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="NickeltownPOSV4.Views.Panels.PitstopHeldSalesPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="using:NickeltownPOSV4.Converters"
    xmlns:vm="using:NickeltownPOSV4.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Padding="12,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="280" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                FontSize="22"
                FontWeight="SemiBold"
                Foreground="{StaticResource PosTextPrimaryBrush}"
                Text="Held sales" />
            <Button
                Grid.Column="1"
                MinWidth="72"
                MinHeight="36"
                Command="{Binding CloseCommand}"
                Content="Close"
                Style="{StaticResource TabsHeaderActionButtonStyle}" />
        </Grid>
        <TextBlock
            Grid.Row="1"
            Margin="0,6,0,0"
            FontSize="13"
            Foreground="{StaticResource PosTextSecondaryBrush}"
            Text="{Binding PageLabel, Mode=OneWay}" />
        <TextBlock
            Grid.Row="2"
            Margin="0,6,0,0"
            FontSize="12"
            Foreground="{StaticResource PosBalanceNegativeBrush}"
            Text="{Binding StatusMessage, Mode=OneWay}"
            TextWrapping="WrapWholeWords"
            Visibility="{Binding HasStatusMessage, Converter={StaticResource BoolToVis}, Mode=OneWay}" />
        <ListView
            Grid.Row="3"
            Margin="0,8,0,0"
            HorizontalAlignment="Stretch"
            ItemsSource="{Binding Items, Mode=OneWay}"
            SelectedItem="{Binding Selected, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:PitstopHeldSaleRowViewModel">
                    <Grid Padding="4,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock
                                FontSize="14"
                                FontWeight="SemiBold"
                                Foreground="{StaticResource PosTextPrimaryBrush}"
                                Text="{x:Bind DisplayLabel, Mode=OneTime}"
                                TextTrimming="CharacterEllipsis" />
                            <TextBlock
                                Margin="0,2,0,0"
                                FontSize="12"
                                Foreground="{StaticResource PosTextSecondaryBrush}"
                                Text="{x:Bind SubtitleText, Mode=OneTime}"
                                TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                        <TextBlock
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            FontSize="15"
                            FontWeight="Bold"
                            Foreground="{StaticResource PosTextPrimaryBrush}"
                            Text="{x:Bind BalanceLine, Mode=OneTime}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel
            Grid.Row="4"
            Margin="0,8,0,0"
            Orientation="Horizontal">
            <Button
                MinWidth="120"
                MinHeight="44"
                Margin="0,0,6,0"
                Command="{Binding RecallCommand}"
                Content="Recall to cart"
                Style="{StaticResource TabsPagerButtonStyle}" />
            <Button
                MinWidth="120"
                MinHeight="44"
                Command="{Binding DiscardCommand}"
                Content="Discard"
                Style="{StaticResource TabsHeaderActionButtonStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
